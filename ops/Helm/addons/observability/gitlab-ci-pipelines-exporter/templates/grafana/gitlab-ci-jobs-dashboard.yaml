apiVersion: v1
kind: ConfigMap
metadata:
  name: gitlab-ci-jobs-dashboard
  labels:
    grafana_dashboard: "true"
  annotations:
    grafana_dashboard_folder: GitLab
data:
  gitlab-ci-jobs-dashboard.json.url: "https://grafana.com/api/dashboards/13328/revisions/latest/download"
