# Common default values for cassandra-operator (k8ssandra-operator).
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
#
# The bundled cass-operator uses admission webhooks and expects cert-manager in the cluster.
# For local rendering: helm template ... --api-versions=cert-manager.io/v1
# Or under k8ssandra-operator.cass-operator: disableCertManagerCheck: true (not for production).

k8ssandra-operator:
  enabled: true
  # Pre-upgrade CRD job pulls chart by name from repo; skip if it blocks sync (CRDs still ship with the chart).
  disableCrdUpgraderJob: true

  resources:
    requests:
      cpu: 15m
      memory: 30Mi
